{{- if not (stat (joinPath .chezmoi.homeDir ".config" "chezmoi-age" "encrypt.key")) -}}
# Skip encrypted files when the encryption key isn't present.
**/*.age
.local/bin/kasi
.local/bin/kasinode
{{- end -}}

container
